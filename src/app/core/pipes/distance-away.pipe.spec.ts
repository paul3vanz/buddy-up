import { DistanceAwayPipe } from './distance-away.pipe';

describe('DistanceAwayPipe', () => {
  it('create an instance', () => {
    const pipe = new DistanceAwayPipe();
    expect(pipe).toBeTruthy();
  });
});
